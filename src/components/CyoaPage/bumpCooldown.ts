// Manual bump cooldown (mirrors the Go check in game_edits.go). Own module so the lazy edit dialog
// isn't pulled into the game page chunk by this one constant.
export const BUMP_COOLDOWN_MS = 14 * 24 * 60 * 60 * 1000;
